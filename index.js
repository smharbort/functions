/*
                                                        JS FUNCTION CORRECT SYNTAXES

function functionName (parameters) {}

const variableName = function (parameters) {}

const variableName = (parameters) => expression         // is the same as 
                                                        // const add = (a, b) => a + b
                                                        // without

note: as far as I understand, code in-function cannot execute after return statements

*/

